/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Permite usar a sintaxe moderna do three.js com o transpile do Next.
  transpilePackages: ["three"],
};

export default nextConfig;
